#include <stdio.h>

/**
 *	print_alphabet - print all alphabet in lowercase
 *
 *	Return: Always 0.
 */
void print_alphabet(void)
{
	char alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
		_putchar(letter);
	-putchar('\n');
}
