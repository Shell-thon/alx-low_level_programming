#include "main.h"
/**
 *  add - function
 *  @a: first operand
 *  @b: second operand
 *
 * Description: function that adds two integers and returns the result.
 * Return: Always 0 (success)
 */
int add(int a, int b)
{
	return (a + b);
}
